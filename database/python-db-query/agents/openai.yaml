interface:
  display_name: "Python DB Query"
  short_description: "Run safe Python database queries"
  default_prompt: "Use this skill to run a database query from Python. If no config file exists, ask me for the database connection details and keep them in a local config file."
