# GLM Entry

Read `AGENTS.md`, choose the matching skill from `docs/skill-index.md`, then read the selected `SKILL.md` completely and follow it.

If automatic skill discovery is unavailable, use the explicit paths in the catalog. Detailed guidance lives in `docs/platform-compatibility.md`.
