interface:
  display_name: "Office CLI"
  short_description: "Create and edit Office files with structured CLI operations"
  default_prompt: "Use $office-cli-skill to inspect this Office file, make the requested changes, validate the result, and preserve the source unless I ask to overwrite it."
