interface:
  display_name: "Python Offline Dependency Packager"
  short_description: "Download pinned Python packages for offline installation"
  default_prompt: "Use $python-offline-dependency-packager to ask for an exact package version and target Python X.Y or X.Y.x line, then create one-click offline installers with all transitive dependencies."
