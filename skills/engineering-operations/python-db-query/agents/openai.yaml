interface:
  display_name: "Database & MinIO Query"
  short_description: "Query databases and MinIO object storage safely"
  default_prompt: "Use $python-db-query to inspect database metadata, run a safe query, or inspect MinIO buckets and objects. If no config exists, ask me for the required connection details before creating one."
