interface:
  display_name: "Draw.io Code Dependencies"
  short_description: "Analyze code dependencies as draw.io graphs"
  default_prompt: "Use $drawio-skill only to analyze import, module, or package dependencies in a codebase and generate an editable draw.io dependency graph."
