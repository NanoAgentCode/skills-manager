interface:
  display_name: "Finishing A Development Branch"
  short_description: "Choose merge, PR, or cleanup next steps"
  default_prompt: "Use $finishing-a-development-branch when implementation is complete, tests pass, and the work needs merge, PR, or cleanup decisions."
