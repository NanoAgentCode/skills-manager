interface:
  display_name: "Using Git Worktrees"
  short_description: "Create isolated workspaces for feature work"
  default_prompt: "Use $using-git-worktrees when starting feature work that needs isolation from the current workspace or before executing implementation plans."
