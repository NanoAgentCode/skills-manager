# Claude Entry

Read `AGENTS.md`, choose the matching skill from `docs/skill-index.md`, then read the selected `SKILL.md` completely and follow it.

Detailed Claude and cross-platform guidance lives in `docs/platform-compatibility.md`. Treat `agents/openai.yaml` as optional UI metadata.
